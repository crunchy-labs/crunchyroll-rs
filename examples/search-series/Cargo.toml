[package]
name = "search-series"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
crunchyroll-rs = { path = "../.." }

tokio = { workspace = true, features = ["macros", "rt", "rt-multi-thread"] }
