[package]
name = "release-calendar"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
crunchyroll-rs = { path = "../..", features = ["release-calendar"] }

chrono = "0.4"
tokio = { version = "1.52", features = ["macros", "rt", "rt-multi-thread"] }
