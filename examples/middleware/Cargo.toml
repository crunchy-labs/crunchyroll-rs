[package]
name = "middleware"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
crunchyroll-rs = { path = "../..", features = ["middleware"] }
reqwest = "0.13"
serde_json = "1.0"
tower-service = "0.3"
tokio = { version = "1.52", features = ["macros", "rt", "rt-multi-thread"] }
